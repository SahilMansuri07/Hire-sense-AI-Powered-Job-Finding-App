
  # Design HireSense AI Prototype

  This is a code bundle for Design HireSense AI Prototype. The original project is available at https://www.figma.com/design/lDl6BWJCoY7EdWGxDRO96K/Design-HireSense-AI-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  

// For Backend
.venv\Scripts\python.exe -m uvicorn main:app --reload --host 127.0.0.1 --port 8000